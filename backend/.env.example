# Exemplo de configuração para o backend do sistema-fon
# Copie para .env e ajuste valores antes de rodar: cp .env.example .env

# Porta em que o servidor irá escutar (padrão no código: 3000)
PORT=3000

# Segredo usado para assinar tokens JWT (use um valor forte em produção)
JWT_SECRET=troque_por_um_segredo_bem_grande

# Timezone usado para timestamps (padrão: America/Sao_Paulo)
TZ=America/Sao_Paulo

# Origens permitidas para CORS (separadas por vírgula)
# Ex: CORS_ORIGINS=http://localhost:8000,https://seu-app.netlify.app
CORS_ORIGINS=http://localhost:8000

# Diretório onde os dados e backups serão salvos (padrão: ./data)
DATA_DIR=./data

# Senhas seed para contas padrão (apenas para desenvolvimento)
SEED_ADMIN_PASSWORD=admin123
SEED_MANAGER_PASSWORD=gerente123

# Observação: o Dockerfile expõe a porta 10000, ajuste se necessário.
